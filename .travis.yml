language: java
jdk:
  - oraclejdk8
#  - oraclejdk7
#  - openjdk7
before_install:
  - pip install codecov
install: true
sudo: false
script: 
#  - sudo apt-get update && sudo apt-get install oracle-java8-installer
#  - java -version
  - mvn clean verify test
after_success:
  - codecov
  
  